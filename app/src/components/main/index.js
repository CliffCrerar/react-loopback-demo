import React from 'react';
import {style} from './index.scss';

const Main = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Main.propTypes = {

};

export default Main;
