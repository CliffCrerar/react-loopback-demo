import React from 'react';
import {style} from './index.scss';

const Footer = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Footer.propTypes = {

};

export default Footer;
