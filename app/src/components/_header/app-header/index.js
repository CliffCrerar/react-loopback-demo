import React from 'react';
import {style} from './index.scss';

const AppHeader = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

AppHeader.propTypes = {

};

export default AppHeader;
