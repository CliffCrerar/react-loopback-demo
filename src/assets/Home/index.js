import React from 'react';
import {style} from './index.scss';

const Home = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Home.propTypes = {

};

export default Home;
