import React from 'react';
import {style} from './index.scss';

const PageOne = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

PageOne.propTypes = {

};

export default PageOne;
